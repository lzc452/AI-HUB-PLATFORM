route:
  receiver: oncall-prod
  group_by: ["alertname", "category"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: oncall-prod
    webhook_configs:
      - url: https://alerts.internal.example/ai-hub
        send_resolved: true
