# 安全审查其余项修复 — 总览

处理 `tmp/security-and-bug-review.md` 中除高危-1/2/3 外的剩余项。方法论：**报告可能已过时，须逐项实证而非假设；同类问题全量扫描；未越界改动；真实活 bug 修完按复现流程做真实测试。**

## 落到代码的真实活 bug（3 处）
- **中危-5** `problem-details.filter.ts`：`detail` 原样回显 → 新增 `escapeHtml` 转义，6/6 单测通过。
- **中危-4** `dingtalk-api.client.ts`：SSRF 防护死代码 → 接入 `assertPublicHttpTarget`，4/4 单测通过。
- **低危-6/7** Outbox 幂等键用 `randomUUID()` + 缺 `ON CONFLICT` → 4 个 repository 加 `idempotencyKey` 与 `onConflict(...).doNothing()`，5 个业务事件接线稳定键，29 例无回归。

## 经实证确认报告已过时 / 需架构决策（6 项，未越界改动）
- **中危-6** 重放 nonce 绑定真实用户：中间件早于守卫、读请求头可伪造 → 需架构决策，未做不安全改动。
- **严重-1/2** 审核误下架：代码已实现 `pendingVersionId` + `sourceStatus` 回滚。
- **中危-3** review 队列终态：`completeReviewQueue` 已置 `completed`。
- **中危-4biz** owner 可见性：`requireVisibleOrManageable` 已放行 owner/maintainer/APPLICATION_MANAGE。
- **中危-5biz** 并发双赞：`application_likes` 复合主键 + `onConflict doNothing` 在 DB 层阻止。

## 验证
- `tsc` 全量 0 错；4 个测试套件 **39/39** 通过。

## 交付物
- 完整报告：`docs/安全审查其余项修复报告.md`
